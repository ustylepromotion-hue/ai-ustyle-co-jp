# AI-USTYLE Pages デプロイ保護

対象: `ai-ustyle-co-jp-web`

## 今回の復旧

2026-09-04、メインページを最新のローカル正本から復旧しました。

Production:
`https://59488758.ai-ustyle-co-jp-web.pages.dev`

## 保護内容

`public/` 全体を無条件に再デプロイしません。

`/scripts/pages_safe_deploy.mjs` は、次を実行します。

1. ローカルのメインページと依存アセット12パスをSHA-256検証
2. 現行Productionの全manifestをCloudflare APIから取得
3. 既存manifestを全量保持
4. 保護対象12パスだけを正本で置換
5. `_redirects` のlifecreate / transportation / careルールを検証して保持
6. Previewを先に作成
7. Previewの回帰検証後にProductionを作成

manifest上のファイル数は今回、53 → 53で増減なしです。
既存ファイル削除は行っていません。

## 回帰ゲート

トップ保護対象:
`node scripts/verify_pages_main_protection.mjs https://www.ai-ustyle.co.jp`

既存ページ:
`node scripts/verify_existing_paths.mjs https://www.ai-ustyle.co.jp`

検証対象:

- `/`
- `/pricing.html`
- `/company.html`
- `/usecase-ebay.html`
- `/test/ai/`
- `/transportation/`
- `/lifecreate/`
- `/care/`

## 今後のルール

- 他ページをデプロイするときも、必ず現行Production manifestを保持する
- 古いworktreeから `wrangler pages deploy public` を実行しない
- Preview回帰検証なしにProductionへ反映しない
- `/index.html`、`/main.js`、`/style.css`、トップ依存アセットを旧版で上書きしない
- トップの正本を変更した場合は `scripts/pages-main-protection.json` のSHAを更新し、Previewで検証してからProductionへ反映する
- 通常のdeployコマンドではなく、次のスクリプトを使う

```text
node scripts/pages_safe_deploy.mjs plan
node scripts/pages_safe_deploy.mjs preview
node scripts/verify_pages_main_protection.mjs https://<preview>.ai-ustyle-co-jp-web.pages.dev
node scripts/verify_existing_paths.mjs https://<preview>.ai-ustyle-co-jp-web.pages.dev
node scripts/pages_safe_deploy.mjs production
node scripts/verify_pages_main_protection.mjs https://www.ai-ustyle.co.jp
node scripts/verify_existing_paths.mjs https://www.ai-ustyle.co.jp
```

注意: AGENTS.mdは保護されたエージェント指示ファイルのため、今回の自動編集は拒否されました。運用ルールの正本はこのファイルと `scripts/` 配下です。
